window.onload = function() {
    const pi = 3.142;
    // pi = 10;
    // const pi = 10;
    // console.log(pi);

    function calcArea(r) {
        const pi = 10;
        console.log("The area is: " + pi * r * r);
    }

    console.log(pi);
    calcArea(5);
}